<!DOCTYPE html>
<html>
  <head>
    <title>Gemle</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="
    dark:bg-[conic-gradient(at_top_right,_var(--tw-gradient-stops))] dark:from-stone-900 dark:via-slate-950 dark:to-stone-800
    bg-[conic-gradient(at_right,_var(--tw-gradient-stops))] from-slate-600 via-slate-300 to-slate-200
    antialiased
  ">
  
    <%= render "shared/navbar" %> 
    <div class="mx-auto max-w-5xl flex flex-col min-h-screen font-sans">
      <main class="flex-grow">
        <%= yield%>
      </main>
      <%= render "shared/footer" %>
    </div>
  </body>

</html>
